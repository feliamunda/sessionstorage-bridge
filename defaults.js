// Optional: pre-fill the popup's "Keys to bridge" field on first install.
// Leave this empty to ship with no defaults - the popup falls back to an
// empty input and you type/save your own keys once, which then persist in
// the extension's own storage regardless of what's in this file.
//
// To pre-fill your own defaults (e.g. for your team, so nobody has to type
// keys on first install), list the sessionStorage key names here:
//
//   const DEFAULT_KEYS = ['access_token', 'refresh_token', 'user_data'];
//
const DEFAULT_KEYS = [];
